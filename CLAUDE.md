# Last Hit Kingdom

Use the vanilla HTML/CSS/JavaScript starter-compatible structure. Keep game rules in `js/game/**` pure modules, keep Firebase-facing code in `js/net.js`, and validate core changes with `node --test` plus `node scripts/simulate.mjs`.
