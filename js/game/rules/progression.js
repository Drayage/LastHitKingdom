import { BALANCE_CONFIG } from '../config/balance.js';
export function applyCapitalPass(state, playerId) { const p = state.players.find(x=>x.id===playerId); p.lapCount += 1; p.troops += BALANCE_CONFIG.capitalSupply; if (p.lapCount === 1 && !p.jobId) p.jobId = 'knight'; else if (p.lapCount > 1) p.level += 1; state.actionLog.push({ type:'CAPITAL_PASS', playerId, lapCount:p.lapCount, troops:p.troops, level:p.level }); }
