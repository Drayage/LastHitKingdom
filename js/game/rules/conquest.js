import { scaledValue } from '../config/balance.js?v=0.2.1';
import { calculateFriendlySupport, levelConfig } from './support.js';
export function calculateConquestRequirement(state, territoryId) { const t = state.territories[territoryId]; if (!t.ownerId) return 0; const base=scaledValue(levelConfig(t).conquestCost, t.line) + calculateFriendlySupport(state, territoryId, t.ownerId); return Math.round(base*(state.economyMultiplier??1)); }
export function conquerTerritory(state, attackerId, territoryId, directTroops) { const attacker = state.players.find(p=>p.id===attackerId); const t = state.territories[territoryId]; if (!attacker || !t || !t.ownerId || t.ownerId === attackerId || directTroops > attacker.troops) throw new Error('Invalid conquest'); const defender = state.players.find(p=>p.id===t.ownerId); attacker.troops -= directTroops; const attackForce = directTroops + calculateFriendlySupport(state, territoryId, attackerId); const requirement = calculateConquestRequirement(state, territoryId); const success = attackForce >= requirement; if (success) { defender.ownedTerritoryIds = defender.ownedTerritoryIds.filter(id=>id!==territoryId); t.ownerId = attackerId; attacker.ownedTerritoryIds.push(territoryId); } state.actionLog.push({ type:'CONQUEST', attackerId, territoryId, directTroops, attackForce, requirement, success }); return { attackForce, requirement, success }; }
