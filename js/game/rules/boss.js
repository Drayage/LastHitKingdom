import { BALANCE_CONFIG } from '../config/balance.js';
export function calculateBossDamage(state, playerId) { const p = state.players.find(x=>x.id===playerId); return p.ownedTerritoryIds.reduce((sum,id)=> sum + (BALANCE_CONFIG.territoryLevel[state.territories[id].level]?.bossDamageValue ?? 0), 0) + (p.acquiredTraitIds.includes('boss-hunter') ? 2 : 0); }
export function damageBoss(state, playerId) { const damage = calculateBossDamage(state, playerId); if (damage <= 0) return { damage: 0, defeated: false }; state.boss.currentHp = Math.max(0, state.boss.currentHp - damage); state.boss.lastDamagedByPlayerId = playerId; const defeated = state.boss.currentHp === 0; if (defeated) { state.phase='GAME_OVER'; state.winnerId=playerId; state.victoryType='BOSS_KILL'; } state.actionLog.push({ type:'BOSS_DAMAGE', playerId, damage, defeated }); return { damage, defeated }; }
